export enum SetupStoreId {
  App = 'app-store',
  Theme = 'theme-store',
  Auth = 'auth-store',
  Route = 'route-store',
  Tab = 'tab-store'
}

/**
 * @description: 公共文案
 */
export enum UNIFORM_TEXT {
  NULL = '--',
}
